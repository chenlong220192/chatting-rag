# ============================================================
# Web Layer Configuration
# ============================================================
web:
  cors:
    allowed-origins:
      - http://localhost:*
      - http://127.0.0.1:*

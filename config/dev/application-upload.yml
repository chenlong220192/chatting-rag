# ============================================================
# 上传配置
# 管理用户上传的原始文档文件的本地存储路径。
#
# dir: 文档文件的存储根目录，支持相对路径（相对于项目根目录）
#   - ./data/documents: 项目根目录下的 data/documents 文件夹
#   - 绝对路径: /var/data/rag/documents（生产环境建议使用）
# ============================================================
upload:
  dir: ./data/documents
